{
  "name": "iffclan",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "sass --no-source-map --watch sass:css --style compressed"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sass": "^1.35.1"
  }
}
